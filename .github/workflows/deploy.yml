name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          command_timeout: 30m
          script_stop: true
          script: |
            export DOCKER_HOST=unix:///tank/services/docker.sock
            cd /home/neurobomber/Apps/vibes/botc-grimoire-companion
            git fetch origin
            # Move the local branch pointer to match origin without touching the
            # working tree, since the clone below only sees this dir's local
            # branches, not its origin/* tracking refs.
            git update-ref refs/heads/main origin/main

            # Build the new image in a clean temporary directory to prevent local changes from blocking checkout
            rm -rf /tmp/botc-grimoire-companion-prod-build
            git clone --no-checkout file:///home/neurobomber/Apps/vibes/botc-grimoire-companion /tmp/botc-grimoire-companion-prod-build
            cd /tmp/botc-grimoire-companion-prod-build
            git checkout origin/main
            cp /home/neurobomber/Apps/vibes/botc-grimoire-companion/docker-compose.prod.yml .
            cp /home/neurobomber/Apps/vibes/botc-grimoire-companion/docker-compose.dev.yml .
            cp /home/neurobomber/Apps/vibes/botc-grimoire-companion/.env .
            BUILDKIT_PROGRESS=plain docker compose -f docker-compose.prod.yml build
            
            # Switch back to the main directory, clean up the build directory, and update the container
            cd /home/neurobomber/Apps/vibes/botc-grimoire-companion
            rm -rf /tmp/botc-grimoire-companion-prod-build
            docker compose -f docker-compose.prod.yml up -d

