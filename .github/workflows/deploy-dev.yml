name: Deploy to Dev

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          command_timeout: 30m
          script_stop: true
          script: |
            export DOCKER_HOST=unix:///tank/services/docker.sock
            cd /home/neurobomber/Apps-Docker/vibes/botc-grimoire-companion
            git fetch origin
            git checkout dev
            git pull origin dev
            BUILDKIT_PROGRESS=plain docker compose -f docker-compose.dev.yml up -d --build --remove-orphans
